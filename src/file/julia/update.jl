# Escrever sobre o arquivo
# io = open("crud.txt", "w")
# write(io, "\nEstou manipulando arquivo e aprendi o conceito de CRUD") 

# Adicionar ao final do arquivo
io = open("crud.txt", "a")
write(io, "\nEstou manipulando arquivo e aprendi o conceito de CRUD") 